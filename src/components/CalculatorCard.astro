---
const { name, description, url, comingSoon } = Astro.props;
---

<div class="bg-white p-6 rounded-lg shadow-md text-center h-full flex flex-col justify-between">
  <div>
    <h3 class="text-xl font-semibold mb-2">{name}</h3>
    <p class="text-neutral-600 mb-4">{description}</p>
  </div>
  {comingSoon ? (
    <div class="mt-4">
      <span class="inline-block bg-gray-200 text-gray-700 py-2 px-4 rounded-full font-semibold">Coming Soon</span>
    </div>
  ) : (
    <a href={url} class="inline-block bg-primary-500 text-white py-2 px-4 rounded-full font-semibold hover:bg-primary-600 transition duration-300">Try it Now</a>
  )}
</div>