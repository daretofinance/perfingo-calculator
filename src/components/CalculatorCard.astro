---
const { name, description, url } = Astro.props;
---

<div class="bg-white p-6 rounded-lg shadow-md text-center">
  <h3 class="text-xl font-semibold mb-2">{name}</h3>
  <p class="text-neutral-600 mb-4">{description}</p>
  <a href={url} class="inline-block bg-primary-500 text-white py-2 px-4 rounded-full font-semibold hover:bg-primary-600">Try it Now</a>
</div>
